'use strict';

const assert = require('chai').assert;
const selector = require('../../../lib/composites/landmarks-menu/selector');

describe('composites/landmarks-menu/selector', () => {
  it('should be a string', () => assert.equal('string', typeof selector));
});
